{
  "name": "backend",
  "version": "1.0.0",
  "description": "CET geoguessr challenge backend",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "dev": "npx nodemon --ignore node_modules/ --ignore '*.db' --ignore '*.db-wal' --ignore '*.db-shm' index.js",
    "start": "node index.js"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "allowScripts": {
    "better-sqlite3@13.0.3": true
  }
}
